# Project Rules

- **Auto-sync to GitHub**: Whenever any code changes, new features, or updates are completed, automatically stage, commit, and push the changes to GitHub (`git add .`, `git commit`, `git push origin main`).
